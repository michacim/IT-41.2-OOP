# ContactsFX
## Version 0.1




## Aufgabe
1. implementiere in ContactServiceFile :   public List<Contact> findByName(String name) 
    mir hilfe der Methode ContactServiceFile.findAll()
    - nach Teilstring suchen können
    - FileHandler wird nicht benötigt
2. Zusatz: implementiere ContactServiceFile.deleteById
   public boolean deleteById(int id) 

        //1. Liste holen : readContacts()
        //2. aus Liste löschen
        //3. Datei neu schreiben